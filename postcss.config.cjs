/* PostCSS config — Tailwind v4 is handled by the Vite plugin (@tailwindcss/vite)
   so only autoprefixer is listed here for vendor-prefix support. */
module.exports = {
  plugins: {
    autoprefixer: {},
  },
};
